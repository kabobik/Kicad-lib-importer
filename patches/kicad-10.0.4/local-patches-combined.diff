diff --git a/common/font/outline_font.cpp b/common/font/outline_font.cpp
index 803f2f3..780e898 100644
--- a/common/font/outline_font.cpp
+++ b/common/font/outline_font.cpp
@@ -153,8 +153,11 @@ double OUTLINE_FONT::GetInterline( double aGlyphHeight, const METRICS& aFontMetr
 {
     double glyphToFontHeight = 1.0;
 
-    if( GetFace()->units_per_EM )
-        glyphToFontHeight = GetFace()->height / GetFace()->units_per_EM;
+    if( GetFace() && GetFace()->units_per_EM > 0 && GetFace()->height > 0 )
+    {
+        glyphToFontHeight = static_cast<double>( GetFace()->height )
+                            / static_cast<double>( GetFace()->units_per_EM );
+    }
 
     return aFontMetrics.GetInterline( aGlyphHeight * glyphToFontHeight );
 }
diff --git a/common/lib_tree_model.cpp b/common/lib_tree_model.cpp
index 938e5f9..a340c33 100644
--- a/common/lib_tree_model.cpp
+++ b/common/lib_tree_model.cpp
@@ -259,6 +259,37 @@ void LIB_TREE_NODE_ITEM::UpdateScore( const std::vector<std::unique_ptr<EDA_COMB
 }
 
 
+LIB_TREE_NODE_GROUP::LIB_TREE_NODE_GROUP( LIB_TREE_NODE* aParent, const wxString& aGroupName )
+{
+    m_Type = TYPE::GROUP;
+    m_Parent = aParent;
+    m_Name = aGroupName;
+    m_Desc = wxEmptyString;
+    m_LibId = aParent->m_LibId;
+}
+
+
+void LIB_TREE_NODE_GROUP::AddChild( std::unique_ptr<LIB_TREE_NODE> aChild )
+{
+    aChild->m_Parent = this;
+    m_Children.push_back( std::move( aChild ) );
+}
+
+
+void LIB_TREE_NODE_GROUP::UpdateScore(
+        const std::vector<std::unique_ptr<EDA_COMBINED_MATCHER>>& aMatchers,
+        std::function<bool( LIB_TREE_NODE& aNode )>* aFilter )
+{
+    m_Score = 0;
+
+    for( std::unique_ptr<LIB_TREE_NODE>& child : m_Children )
+    {
+        child->UpdateScore( aMatchers, aFilter );
+        m_Score = std::max( m_Score, child->m_Score );
+    }
+}
+
+
 LIB_TREE_NODE_LIBRARY::LIB_TREE_NODE_LIBRARY( LIB_TREE_NODE* aParent, wxString const& aName,
                                               wxString const& aDesc )
 {
diff --git a/common/lib_tree_model_adapter.cpp b/common/lib_tree_model_adapter.cpp
index ac8ebff..1af3ce5 100644
--- a/common/lib_tree_model_adapter.cpp
+++ b/common/lib_tree_model_adapter.cpp
@@ -178,6 +178,8 @@ void LIB_TREE_MODEL_ADAPTER::loadColumnConfig()
 
     m_shownColumns = m_cfg.columns;
 
+    m_groupByColumn = m_cfg.group_by_column;
+
     if( m_shownColumns.empty() )
         m_shownColumns = {  _HKI( "Item" ), _HKI( "Description" ) };
 
@@ -232,6 +234,7 @@ void LIB_TREE_MODEL_ADAPTER::SaveSettings()
         }
 
         m_cfg.open_libs = GetOpenLibs();
+        m_cfg.group_by_column = m_groupByColumn;
     }
 }
 
@@ -384,6 +387,11 @@ void LIB_TREE_MODEL_ADAPTER::AttachTo( wxDataViewCtrl* aDataViewCtrl )
 {
     m_widget = aDataViewCtrl;
     aDataViewCtrl->SetIndent( kDataViewIndent );
+
+    // Apply any saved grouping before associating the model with the control
+    if( !m_groupByColumn.IsEmpty() )
+        rebuildGroupNodes();
+
     aDataViewCtrl->AssociateModel( this );
     recreateColumns();
 }
@@ -561,10 +569,21 @@ wxDataViewItem LIB_TREE_MODEL_ADAPTER::FindItem( const LIB_ID& aLibId )
         if( aLibId.GetLibItemName() == "" )
             return ToItem( lib.get() );
 
-        for( std::unique_ptr<LIB_TREE_NODE>& alias: lib->m_Children )
+        for( std::unique_ptr<LIB_TREE_NODE>& child: lib->m_Children )
         {
-            if( alias->m_Name == aLibId.GetLibItemName().wx_str() )
-                return ToItem( alias.get() );
+            if( child->m_Type == LIB_TREE_NODE::TYPE::GROUP )
+            {
+                for( std::unique_ptr<LIB_TREE_NODE>& item: child->m_Children )
+                {
+                    if( item->m_Name == aLibId.GetLibItemName().wx_str() )
+                        return ToItem( item.get() );
+                }
+            }
+            else
+            {
+                if( child->m_Name == aLibId.GetLibItemName().wx_str() )
+                    return ToItem( child.get() );
+            }
         }
 
         break;  // could not find the part in the requested library
@@ -588,6 +607,7 @@ unsigned int LIB_TREE_MODEL_ADAPTER::GetChildren( const wxDataViewItem& aItem,
 
     if( node->m_Type == LIB_TREE_NODE::TYPE::ROOT
             || node->m_Type == LIB_TREE_NODE::TYPE::LIBRARY
+            || node->m_Type == LIB_TREE_NODE::TYPE::GROUP
             || ( m_show_units && node->m_Type == LIB_TREE_NODE::TYPE::ITEM ) )
     {
         for( std::unique_ptr<LIB_TREE_NODE> const& child: node->m_Children )
@@ -736,7 +756,12 @@ void LIB_TREE_MODEL_ADAPTER::GetValue( wxVariant& aVariant, const wxDataViewItem
         break;
 
     default:
-        if( m_colIdxMap.count( aCol ) )
+        if( node->m_Type == LIB_TREE_NODE::TYPE::GROUP )
+        {
+            // GROUP nodes only show name; other columns are empty
+            valueStr = wxEmptyString;
+        }
+        else if( m_colIdxMap.count( aCol ) )
         {
             const wxString& key = m_colIdxMap.at( aCol );
 
@@ -775,6 +800,14 @@ bool LIB_TREE_MODEL_ADAPTER::GetAttr( const wxDataViewItem& aItem, unsigned int
             return true;
         }
     }
+    else if( node->m_Type == LIB_TREE_NODE::TYPE::GROUP )
+    {
+        if( aCol == 0 )
+        {
+            aAttr.SetBold( true );
+            return true;
+        }
+    }
 
     return false;
 }
@@ -880,3 +913,143 @@ const LIB_TREE_NODE* LIB_TREE_MODEL_ADAPTER::showResults()
 }
 
 
+void LIB_TREE_MODEL_ADAPTER::SetGroupColumn( const wxString& aColumn )
+{
+    if( m_groupByColumn == aColumn )
+        return;
+
+    m_groupByColumn = aColumn;
+    rebuildGroupNodes();
+
+    Freeze();
+    BeforeReset();
+    m_tree.SortNodes( m_sort_mode == BEST_MATCH );
+    AfterReset();
+    Thaw();
+
+    showResults();
+
+#ifdef __WXGTK__
+    m_widget->Refresh();
+    m_widget->Update();
+#endif
+}
+
+
+bool LIB_TREE_MODEL_ADAPTER::GetColumnName( int aColIdx, wxString& aName ) const
+{
+    auto it = m_colIdxMap.find( (unsigned) aColIdx );
+
+    if( it != m_colIdxMap.end() )
+    {
+        aName = it->second;
+        return true;
+    }
+
+    return false;
+}
+
+
+void LIB_TREE_MODEL_ADAPTER::flattenGroups()
+{
+    for( std::unique_ptr<LIB_TREE_NODE>& lib : m_tree.m_Children )
+    {
+        if( lib->m_Type != LIB_TREE_NODE::TYPE::LIBRARY )
+            continue;
+
+        LIB_TREE_NODE::PTR_VECTOR newChildren;
+
+        for( auto& child : lib->m_Children )
+        {
+            if( child->m_Type == LIB_TREE_NODE::TYPE::GROUP )
+            {
+                // Move items from GROUP back into LIBRARY
+                for( auto& item : child->m_Children )
+                {
+                    item->m_Parent = lib.get();
+                    newChildren.push_back( std::move( item ) );
+                }
+            }
+            else
+            {
+                newChildren.push_back( std::move( child ) );
+            }
+        }
+
+        lib->m_Children = std::move( newChildren );
+    }
+}
+
+
+void LIB_TREE_MODEL_ADAPTER::rebuildGroupNodes()
+{
+    // Always start by flattening any existing grouping
+    flattenGroups();
+
+    if( m_groupByColumn.IsEmpty() )
+        return;
+
+    for( std::unique_ptr<LIB_TREE_NODE>& lib : m_tree.m_Children )
+    {
+        if( lib->m_Type != LIB_TREE_NODE::TYPE::LIBRARY )
+            continue;
+
+        // Build a map: groupKey -> list of indices
+        std::map<wxString, std::vector<size_t>> groupMap;
+
+        for( size_t i = 0; i < lib->m_Children.size(); ++i )
+        {
+            LIB_TREE_NODE* item = lib->m_Children[i].get();
+
+            if( item->m_Type != LIB_TREE_NODE::TYPE::ITEM )
+                continue;
+
+            wxString key;
+
+            if( m_groupByColumn == wxT( "Description" ) )
+                key = item->m_Desc;
+            else if( item->m_Fields.count( m_groupByColumn ) )
+                key = item->m_Fields.at( m_groupByColumn );
+
+            if( key.IsEmpty() )
+                key = _( "(ungrouped)" );
+
+            groupMap[key].push_back( i );
+        }
+
+        // If all items fall into one group, don't bother grouping
+        if( groupMap.size() <= 1 )
+            continue;
+
+        // Create GROUP nodes and move ITEM nodes into them
+        LIB_TREE_NODE::PTR_VECTOR newChildren;
+
+        // Preserve any non-ITEM children first (shouldn't happen, but be safe)
+        for( auto& child : lib->m_Children )
+        {
+            if( child && child->m_Type != LIB_TREE_NODE::TYPE::ITEM )
+                newChildren.push_back( std::move( child ) );
+        }
+
+        for( auto& [groupName, indices] : groupMap )
+        {
+            auto group = std::make_unique<LIB_TREE_NODE_GROUP>( lib.get(), groupName );
+
+            for( size_t idx : indices )
+            {
+                if( lib->m_Children[idx] )
+                    group->AddChild( std::move( lib->m_Children[idx] ) );
+            }
+
+            // Propagate existing scores from children so GetChildren() doesn't
+            // filter out the new GROUP node (it checks m_Score > 0).
+            for( const auto& child : group->m_Children )
+                group->m_Score = std::max( group->m_Score, child->m_Score );
+
+            group->AssignIntrinsicRanks( m_shownColumns );
+            newChildren.push_back( std::move( group ) );
+        }
+
+        lib->m_Children = std::move( newChildren );
+    }
+}
diff --git a/common/settings/app_settings.cpp b/common/settings/app_settings.cpp
index 0f54aaf..065d9cb 100644
--- a/common/settings/app_settings.cpp
+++ b/common/settings/app_settings.cpp
@@ -169,6 +169,9 @@ APP_SETTINGS_BASE::APP_SETTINGS_BASE( const std::string& aFilename, int aSchemaV
     m_params.emplace_back(
             new PARAM_LIST<wxString>( "lib_tree.open_libs", &m_LibTree.open_libs, {} ) );
 
+    m_params.emplace_back( new PARAM<wxString>( "lib_tree.group_by_column",
+            &m_LibTree.group_by_column, wxT( "" ) ) );
+
     m_params.emplace_back( new PARAM<bool>( "printing.background",
             &m_Printing.background, false ) );
 
diff --git a/common/view/wx_view_controls.cpp b/common/view/wx_view_controls.cpp
index ceea0e1..c7df21b 100644
--- a/common/view/wx_view_controls.cpp
+++ b/common/view/wx_view_controls.cpp
@@ -363,22 +363,10 @@ void WX_VIEW_CONTROLS::onMotion( wxMouseEvent& aEvent )
         else if( m_state == DRAG_ZOOMING )
         {
             static bool justWarped = false;
-            int         warpY = 0;
-            wxSize      parentSize = m_parentPanel->GetClientSize();
-
-            if( y < 0 )
-            {
-                warpY = parentSize.y;
-            }
-            else if( y >= parentSize.y )
-            {
-                warpY = -parentSize.y;
-            }
 
             if( !justWarped )
             {
                 VECTOR2D d = m_dragStartPoint - mousePos;
-                m_dragStartPoint = mousePos;
 
                 double scale = exp( d.y * m_settings.m_zoomSpeed * 0.001 );
 
@@ -386,18 +374,13 @@ void WX_VIEW_CONTROLS::onMotion( wxMouseEvent& aEvent )
 
                 m_view->SetScale( m_view->GetScale() * scale, m_view->ToWorld( m_zoomStartPoint ) );
                 aEvent.StopPropagation();
-            }
 
-            if( warpY )
-            {
-                if( !justWarped )
-                {
-                    KIPLATFORM::UI::WarpPointer( m_parentPanel, x, y + warpY );
-                    m_dragStartPoint += VECTOR2D( 0, warpY );
-                    justWarped = true;
-                }
-                else
-                    justWarped = false;
+                // Warp cursor back to the original zoom start point so it appears fixed
+                KIPLATFORM::UI::WarpPointer( m_parentPanel,
+                                              (int) m_zoomStartPoint.x,
+                                              (int) m_zoomStartPoint.y );
+                m_dragStartPoint = m_zoomStartPoint;
+                justWarped = true;
             }
             else
             {
@@ -563,6 +546,10 @@ void WX_VIEW_CONTROLS::onButton( wxMouseEvent& aEvent )
             m_zoomStartPoint = m_dragStartPoint;
             setState( DRAG_ZOOMING );
 
+            // Hide the cursor so it appears fixed during drag-zoom
+            m_parentPanel->SetCursor( wxCURSOR_BLANK );
+            KIPLATFORM::UI::InfiniteDragPrepareWindow( m_parentPanel );
+
 #if defined USE_MOUSE_CAPTURE
             if( !m_parentPanel->HasCapture() )
                 m_parentPanel->CaptureMouse();
@@ -575,6 +562,22 @@ void WX_VIEW_CONTROLS::onButton( wxMouseEvent& aEvent )
         break;
 
     case DRAG_ZOOMING:
+        if( aEvent.MiddleUp() || aEvent.LeftUp() || aEvent.RightUp() )
+        {
+            setState( IDLE );
+
+            // Restore the default cursor after drag-zoom
+            m_parentPanel->SetCursor( wxCURSOR_DEFAULT );
+            KIPLATFORM::UI::InfiniteDragReleaseWindow();
+
+#if defined USE_MOUSE_CAPTURE
+            if( !m_settings.m_cursorCaptured && m_parentPanel->HasCapture() )
+                m_parentPanel->ReleaseMouse();
+#endif
+        }
+
+        break;
+
     case DRAG_PANNING:
         if( aEvent.MiddleUp() || aEvent.LeftUp() || aEvent.RightUp() )
         {
diff --git a/common/widgets/lib_tree.cpp b/common/widgets/lib_tree.cpp
index 82365a6..290f81b 100644
--- a/common/widgets/lib_tree.cpp
+++ b/common/widgets/lib_tree.cpp
@@ -715,7 +715,7 @@ void LIB_TREE::onQueryCharHook( wxKeyEvent& aKeyStroke )
     case WXK_ADD:
         updateRecentSearchMenu();
 
-        if( type == LIB_TREE_NODE::TYPE::LIBRARY )
+        if( type == LIB_TREE_NODE::TYPE::LIBRARY || type == LIB_TREE_NODE::TYPE::GROUP )
             m_tree_ctrl->Expand( sel );
 
         break;
@@ -723,7 +723,7 @@ void LIB_TREE::onQueryCharHook( wxKeyEvent& aKeyStroke )
     case WXK_SUBTRACT:
         updateRecentSearchMenu();
 
-        if( type == LIB_TREE_NODE::TYPE::LIBRARY )
+        if( type == LIB_TREE_NODE::TYPE::LIBRARY || type == LIB_TREE_NODE::TYPE::GROUP )
             m_tree_ctrl->Collapse( sel );
 
         break;
@@ -734,7 +734,8 @@ void LIB_TREE::onQueryCharHook( wxKeyEvent& aKeyStroke )
 
         if( GetSelectedLibId().IsValid() )
             postSelectEvent();
-        else if( type == LIB_TREE_NODE::TYPE::LIBRARY )
+        else if( type == LIB_TREE_NODE::TYPE::LIBRARY
+                 || type == LIB_TREE_NODE::TYPE::GROUP )
             toggleExpand( sel );
 
         break;
@@ -1053,7 +1054,41 @@ void LIB_TREE::onHeaderContextMenu( wxDataViewEvent& aEvent )
 
     menu.Add( ACTIONS::selectLibTreeColumns );
 
-    if( GetPopupMenuSelectionFromUser( menu ) != wxID_NONE )
+    // Determine the column that was right-clicked
+    int colIdx = aEvent.GetColumn();
+    wxString clickedColumnName;
+    bool hasClickedColumn = ( colIdx >= 0 && m_adapter->GetColumnName( colIdx, clickedColumnName ) );
+
+    // "Group by '...'" menu item
+    int groupById = wxID_NONE;
+
+    if( hasClickedColumn && clickedColumnName != _HKI( "Item" ) )
+    {
+        menu.AppendSeparator();
+        wxString label = wxString::Format( _( "Group by '%s'" ),
+                                           wxGetTranslation( clickedColumnName ) );
+        wxMenuItem* mi = menu.Append( wxID_ANY, label );
+        groupById = mi->GetId();
+
+        if( m_adapter->GetGroupColumn() == clickedColumnName )
+            mi->Enable( false );
+    }
+
+    // "Remove Grouping" menu item
+    int removeGroupId = wxID_NONE;
+
+    if( !m_adapter->GetGroupColumn().IsEmpty() )
+    {
+        if( !hasClickedColumn || clickedColumnName == _HKI( "Item" ) )
+            menu.AppendSeparator();
+
+        wxMenuItem* mi = menu.Append( wxID_ANY, _( "Remove Grouping" ) );
+        removeGroupId = mi->GetId();
+    }
+
+    int selection = GetPopupMenuSelectionFromUser( menu );
+
+    if( selection == ACTIONS::selectLibTreeColumns.GetUIId() )
     {
         EDA_REORDERABLE_LIST_DIALOG dlg( m_parent, _( "Select Columns" ),
                                          m_adapter->GetAvailableColumns(),
@@ -1065,6 +1100,14 @@ void LIB_TREE::onHeaderContextMenu( wxDataViewEvent& aEvent )
             Regenerate( true );
         }
     }
+    else if( selection == groupById && groupById != wxID_NONE )
+    {
+        m_adapter->SetGroupColumn( clickedColumnName );
+    }
+    else if( selection == removeGroupId && removeGroupId != wxID_NONE )
+    {
+        m_adapter->SetGroupColumn( wxEmptyString );
+    }
 
     m_previewDisabled = false;
 }
diff --git a/eeschema/sch_bus_entry.cpp b/eeschema/sch_bus_entry.cpp
index fc7d694..b3c3258 100644
--- a/eeschema/sch_bus_entry.cpp
+++ b/eeschema/sch_bus_entry.cpp
@@ -657,5 +657,13 @@ static struct SCH_BUS_ENTRY_DESC
 
         propMgr.AddProperty( new PROPERTY<SCH_BUS_ENTRY_BASE, COLOR4D>( _HKI( "Color" ),
                     &SCH_BUS_ENTRY_BASE::SetBusEntryColor, &SCH_BUS_ENTRY_BASE::GetBusEntryColor ) );
+
+        propMgr.AddProperty( new PROPERTY<SCH_BUS_ENTRY_BASE, int>( _HKI( "Size X" ),
+                    &SCH_BUS_ENTRY_BASE::SetSizeX, &SCH_BUS_ENTRY_BASE::GetSizeX,
+                    PROPERTY_DISPLAY::PT_SIZE ) );
+
+        propMgr.AddProperty( new PROPERTY<SCH_BUS_ENTRY_BASE, int>( _HKI( "Size Y" ),
+                    &SCH_BUS_ENTRY_BASE::SetSizeY, &SCH_BUS_ENTRY_BASE::GetSizeY,
+                    PROPERTY_DISPLAY::PT_SIZE ) );
     }
 } _SCH_BUS_ENTRY_DESC;
diff --git a/eeschema/sch_bus_entry.h b/eeschema/sch_bus_entry.h
index 6c22c18..df71e38 100644
--- a/eeschema/sch_bus_entry.h
+++ b/eeschema/sch_bus_entry.h
@@ -73,6 +73,11 @@ public:
     VECTOR2I GetSize() const { return m_size; }
     void SetSize( const VECTOR2I& aSize ) { m_size = aSize; }
 
+    int GetSizeX() const { return m_size.x; }
+    void SetSizeX( int aX ) { m_size.x = aX; }
+    int GetSizeY() const { return m_size.y; }
+    void SetSizeY( int aY ) { m_size.y = aY; }
+
     // Base class getter unused; necessary for property to compile
     int GetPenWidth() const override;
     void SetPenWidth( int aWidth );
diff --git a/eeschema/symbol_tree_synchronizing_adapter.cpp b/eeschema/symbol_tree_synchronizing_adapter.cpp
index f98031f..b2794ea 100644
--- a/eeschema/symbol_tree_synchronizing_adapter.cpp
+++ b/eeschema/symbol_tree_synchronizing_adapter.cpp
@@ -67,7 +67,12 @@ TOOL_INTERACTIVE* SYMBOL_TREE_SYNCHRONIZING_ADAPTER::GetContextMenuTool()
 bool SYMBOL_TREE_SYNCHRONIZING_ADAPTER::IsContainer( const wxDataViewItem& aItem ) const
 {
     const LIB_TREE_NODE* node = ToNode( aItem );
-    return node ? node->m_Type == LIB_TREE_NODE::TYPE::LIBRARY : true;
+
+    if( !node )
+        return true;
+
+    return node->m_Type == LIB_TREE_NODE::TYPE::LIBRARY
+        || node->m_Type == LIB_TREE_NODE::TYPE::GROUP;
 }
 
 
@@ -149,6 +154,10 @@ void SYMBOL_TREE_SYNCHRONIZING_ADAPTER::Sync( const wxString& aForceRefresh,
     }
 
     m_tree.AssignIntrinsicRanks( m_shownColumns );
+
+    // Re-apply grouping if active (updateLibrary flattens groups before syncing).
+    if( !GetGroupColumn().IsEmpty() )
+        rebuildGroupNodes();
 }
 
 
@@ -168,6 +177,30 @@ int SYMBOL_TREE_SYNCHRONIZING_ADAPTER::GetLibrariesCount() const
 
 void SYMBOL_TREE_SYNCHRONIZING_ADAPTER::updateLibrary( LIB_TREE_NODE_LIBRARY& aLibNode )
 {
+    // Flatten any GROUP nodes back to direct ITEM children before syncing.
+    // Groups will be rebuilt at the end of Sync() via rebuildGroupNodes().
+    {
+        LIB_TREE_NODE::PTR_VECTOR flatChildren;
+
+        for( auto& child : aLibNode.m_Children )
+        {
+            if( child->m_Type == LIB_TREE_NODE::TYPE::GROUP )
+            {
+                for( auto& item : child->m_Children )
+                {
+                    item->m_Parent = &aLibNode;
+                    flatChildren.push_back( std::move( item ) );
+                }
+            }
+            else
+            {
+                flatChildren.push_back( std::move( child ) );
+            }
+        }
+
+        aLibNode.m_Children = std::move( flatChildren );
+    }
+
     auto hashIt = m_libHashes.find( aLibNode.m_Name );
 
     if( hashIt == m_libHashes.end() )
diff --git a/eeschema/tools/sch_line_wire_bus_tool.cpp b/eeschema/tools/sch_line_wire_bus_tool.cpp
index 1289c94..68eba34 100644
--- a/eeschema/tools/sch_line_wire_bus_tool.cpp
+++ b/eeschema/tools/sch_line_wire_bus_tool.cpp
@@ -1243,6 +1243,162 @@ void SCH_LINE_WIRE_BUS_TOOL::simplifyWireList()
 }
 
 
+SCH_BUS_WIRE_ENTRY* SCH_LINE_WIRE_BUS_TOOL::tryConvertLastSegmentToBusEntry( SCH_SCREEN* aScreen )
+{
+    // Don't convert during bus unfold operations.
+    if( m_busUnfold.in_progress )
+        return nullptr;
+
+    if( m_wires.empty() )
+        return nullptr;
+
+    auto findDiagonalOnBus = [&]( SCH_LINE* aSeg, VECTOR2I& aBusEnd,
+                                  VECTOR2I& aWireEnd ) -> SCH_LINE*
+    {
+        if( !aSeg || aSeg->GetLayer() != LAYER_WIRE )
+            return nullptr;
+
+        VECTOR2I segStart = aSeg->GetStartPoint();
+        VECTOR2I segEnd = aSeg->GetEndPoint();
+        VECTOR2I delta = segEnd - segStart;
+
+        if( delta.x == 0 || delta.y == 0 )
+            return nullptr;
+
+        if( std::abs( delta.x ) != std::abs( delta.y ) )
+            return nullptr;
+
+        int absDx = std::abs( delta.x );
+
+        static constexpr int MIN_DIAGONAL_COMPONENT = 3500;    // ~0.5 mm / sqrt(2)
+        static constexpr int MAX_DIAGONAL_COMPONENT = 106066;  // ~15 mm / sqrt(2)
+
+        if( absDx < MIN_DIAGONAL_COMPONENT || absDx > MAX_DIAGONAL_COMPONENT )
+            return nullptr;
+
+        int accuracy = schIUScale.MilsToIU( 5 );
+
+        SCH_LINE* bus = aScreen->GetBus( segEnd, accuracy );
+
+        if( bus )
+        {
+            aBusEnd = bus->GetSeg().NearestPoint( segEnd );
+            aWireEnd = segStart;
+            return bus;
+        }
+
+        bus = aScreen->GetBus( segStart, accuracy );
+
+        if( bus )
+        {
+            aBusEnd = bus->GetSeg().NearestPoint( segStart );
+            aWireEnd = segEnd;
+            return bus;
+        }
+
+        return nullptr;
+    };
+
+    SCH_LINE* diagSeg = m_wires.back();
+    int       diagIdx = (int) m_wires.size() - 1;
+    VECTOR2I  busEnd, wireEnd;
+
+    SCH_LINE* bus = findDiagonalOnBus( diagSeg, busEnd, wireEnd );
+
+    if( !bus && m_wires.size() >= 2 )
+    {
+        SCH_LINE* prevSeg = m_wires[m_wires.size() - 2];
+
+        bus = findDiagonalOnBus( prevSeg, busEnd, wireEnd );
+
+        if( bus )
+        {
+            diagSeg = prevSeg;
+            diagIdx = (int) m_wires.size() - 2;
+        }
+    }
+
+    if( !bus && m_wires.size() >= 2 )
+    {
+        SCH_LINE* lastSeg = m_wires.back();
+        SCH_LINE* prevSeg = m_wires[m_wires.size() - 2];
+
+        if( lastSeg && !lastSeg->IsNull() && prevSeg && !prevSeg->IsNull()
+            && lastSeg->GetLayer() == LAYER_WIRE && prevSeg->GetLayer() == LAYER_WIRE )
+        {
+            VECTOR2I lastDelta = lastSeg->GetEndPoint() - lastSeg->GetStartPoint();
+            VECTOR2I prevDelta = prevSeg->GetEndPoint() - prevSeg->GetStartPoint();
+
+            bool lastIsStraight = ( lastDelta.x == 0 || lastDelta.y == 0 );
+            bool prevIs45 = ( prevDelta.x != 0 && prevDelta.y != 0
+                              && std::abs( prevDelta.x ) == std::abs( prevDelta.y ) );
+
+            if( lastIsStraight && prevIs45 )
+            {
+                int      accuracy = schIUScale.MilsToIU( 5 );
+                VECTOR2I lastEnd = lastSeg->GetEndPoint();
+                VECTOR2I lastStart = lastSeg->GetStartPoint();
+
+                SCH_LINE* busLine = aScreen->GetBus( lastEnd, accuracy );
+                VECTOR2I  busPoint = lastEnd;
+
+                if( !busLine )
+                {
+                    busLine = aScreen->GetBus( lastStart, accuracy );
+                    busPoint = lastStart;
+                }
+
+                if( busLine )
+                {
+                    int dxSign = ( prevDelta.x > 0 ) ? 1 : -1;
+                    int dySign = ( prevDelta.y > 0 ) ? 1 : -1;
+                    int diagLen = std::abs( prevDelta.x );
+
+                    VECTOR2I midPoint( busPoint.x - dxSign * diagLen,
+                                       busPoint.y - dySign * diagLen );
+
+                    prevSeg->SetEndPoint( midPoint );
+                    lastSeg->SetStartPoint( midPoint );
+                    lastSeg->SetEndPoint( busPoint );
+
+                    diagSeg = lastSeg;
+                    diagIdx = (int) m_wires.size() - 1;
+                    bus = findDiagonalOnBus( diagSeg, busEnd, wireEnd );
+                }
+            }
+        }
+    }
+
+    if( !bus )
+        return nullptr;
+
+    SCH_BUS_WIRE_ENTRY* busEntry = new SCH_BUS_WIRE_ENTRY( busEnd );
+    busEntry->SetSize( wireEnd - busEnd );
+    busEntry->SetParent( &m_frame->Schematic() );
+
+    m_wires.erase( m_wires.begin() + diagIdx );
+    delete diagSeg;
+
+    if( diagIdx > 0 && diagIdx <= (int) m_wires.size() )
+    {
+        m_wires[diagIdx - 1]->SetEndPoint( wireEnd );
+    }
+    else if( diagIdx == 0 && !m_wires.empty() )
+    {
+        m_wires[0]->SetStartPoint( wireEnd );
+    }
+
+    if( !m_wires.empty() && m_wires.back()->IsNull() )
+    {
+        SCH_LINE* nullSeg = m_wires.back();
+        m_wires.pop_back();
+        delete nullSeg;
+    }
+
+    return busEntry;
+}
+
+
 void SCH_LINE_WIRE_BUS_TOOL::finishSegments( SCH_COMMIT& aCommit )
 {
     // Clear selection when done so that a new wire can be started.
@@ -1255,6 +1411,9 @@ void SCH_LINE_WIRE_BUS_TOOL::finishSegments( SCH_COMMIT& aCommit )
     // Remove segments backtracking over others
     simplifyWireList();
 
+    // Try to auto-convert the last 45° wire segment into a bus entry
+    SCH_BUS_WIRE_ENTRY* autoBusEntry = tryConvertLastSegmentToBusEntry( screen );
+
     // Collect the possible connection points for the new lines
     std::vector<VECTOR2I> connections = screen->GetConnections();
     std::vector<VECTOR2I> new_ends;
@@ -1345,6 +1504,13 @@ void SCH_LINE_WIRE_BUS_TOOL::finishSegments( SCH_COMMIT& aCommit )
     if( m_busUnfold.in_progress )
         m_busUnfold = {};
 
+    if( autoBusEntry )
+    {
+        autoBusEntry->ClearFlags( IS_NEW );
+        m_frame->AddToScreen( autoBusEntry, screen );
+        aCommit.Added( autoBusEntry, screen );
+    }
+
     for( SCH_ITEM* item : m_frame->GetScreen()->Items() )
         item->ClearEditFlags();
 }
diff --git a/eeschema/tools/sch_line_wire_bus_tool.h b/eeschema/tools/sch_line_wire_bus_tool.h
index 4681ce7..8fbd823 100644
--- a/eeschema/tools/sch_line_wire_bus_tool.h
+++ b/eeschema/tools/sch_line_wire_bus_tool.h
@@ -185,6 +185,14 @@ private:
     void computeBreakPoint( const std::pair<SCH_LINE*, SCH_LINE*>& aSegments, VECTOR2I& aPosition,
                             LINE_MODE mode, bool posture );
 
+    /**
+     * Try to convert the last 45° wire segment into a bus entry if it ends on a bus.
+     *
+     * @param aScreen The current schematic screen
+     * @return Pointer to created SCH_BUS_WIRE_ENTRY, or nullptr if conversion not applicable
+     */
+    SCH_BUS_WIRE_ENTRY* tryConvertLastSegmentToBusEntry( SCH_SCREEN* aScreen );
+
 private:
     bool                    m_inDrawingTool;   // Reentrancy guard
 
diff --git a/include/lib_tree_model.h b/include/lib_tree_model.h
index 2183a69..7fcd323 100644
--- a/include/lib_tree_model.h
+++ b/include/lib_tree_model.h
@@ -113,6 +113,7 @@ public:
     {
         ROOT,
         LIBRARY,
+        GROUP,
         ITEM,
         UNIT,
         INVALID
@@ -233,6 +234,33 @@ protected:
 };
 
 
+/**
+ * Node type: group (intermediate grouping node within a library).
+ */
+class LIB_TREE_NODE_GROUP : public LIB_TREE_NODE
+{
+public:
+    LIB_TREE_NODE_GROUP( LIB_TREE_NODE_GROUP const& ) = delete;
+    void operator=( LIB_TREE_NODE_GROUP const& ) = delete;
+
+    /**
+     * Construct a group node.
+     *
+     * @param aParent    parent node (should be a LIBRARY node)
+     * @param aGroupName display name of the group
+     */
+    LIB_TREE_NODE_GROUP( LIB_TREE_NODE* aParent, const wxString& aGroupName );
+
+    /**
+     * Transfer an existing child node into this group (takes ownership).
+     */
+    void AddChild( std::unique_ptr<LIB_TREE_NODE> aChild );
+
+    void UpdateScore( const std::vector<std::unique_ptr<EDA_COMBINED_MATCHER>>& aMatchers,
+                      std::function<bool( LIB_TREE_NODE& aNode )>* aFilter ) override;
+};
+
+
 /**
  * Node type: library
  */
diff --git a/include/lib_tree_model_adapter.h b/include/lib_tree_model_adapter.h
index 21d3afa..34b7a46 100644
--- a/include/lib_tree_model_adapter.h
+++ b/include/lib_tree_model_adapter.h
@@ -201,6 +201,19 @@ public:
         m_lazyLoadHandler = aHandler;
     }
 
+    /**
+     * Set/get the column used for grouping items within each library.
+     * An empty string means no grouping (flat display).
+     */
+    void SetGroupColumn( const wxString& aColumn );
+    wxString GetGroupColumn() const { return m_groupByColumn; }
+
+    /**
+     * Return the internal column name for a given display column index.
+     * @return true if the index was valid and aName was populated.
+     */
+    bool GetColumnName( int aColIdx, wxString& aName ) const;
+
     /**
      * Sets which columns are shown in the widget.  Invalid column names are discarded.
      * @param aColumnNames is an ordered list of column names to show
@@ -409,6 +422,16 @@ protected:
 
     void resortTree();
 
+    /**
+     * Rebuild the GROUP nodes inside each LIBRARY according to m_groupByColumn.
+     */
+    void rebuildGroupNodes();
+
+    /**
+     * Remove all GROUP nodes, moving their ITEM children back to the parent LIBRARY.
+     */
+    void flattenGroups();
+
 private:
     /**
      * Find and expand successful search results.  Return the best match (if any).
@@ -432,6 +455,7 @@ protected:
     std::vector<wxString>        m_availableColumns;
     std::map<wxString, int>      m_colWidths;
     std::vector<wxString>        m_shownColumns;   // Stored in display order
+    wxString                     m_groupByColumn;  ///< Column name for grouping; empty = flat.
     wxDataViewCtrl*              m_widget;
     std::function<void()>        m_lazyLoadHandler;
 
@@ -449,4 +473,3 @@ private:
 };
 
 #endif // LIB_TREE_MODEL_ADAPTER_H
-
diff --git a/include/settings/app_settings.h b/include/settings/app_settings.h
index 1fdbdd9..a8b638f 100644
--- a/include/settings/app_settings.h
+++ b/include/settings/app_settings.h
@@ -158,6 +158,7 @@ public:
         std::vector<wxString> columns;         ///< Ordered list of visible columns in the tree.
         std::map<wxString, int> column_widths; ///< Column widths, keyed by header name.
         std::vector<wxString>   open_libs;     ///< list of libraries the user has open in the tree.
+        wxString                group_by_column; ///< Column name used for grouping (empty = no grouping).
     };
 
     struct PANEL_DESIGN_BLOCK_CHOOSER
diff --git a/pcbnew/fp_tree_synchronizing_adapter.cpp b/pcbnew/fp_tree_synchronizing_adapter.cpp
index 15bc07b..3232bca 100644
--- a/pcbnew/fp_tree_synchronizing_adapter.cpp
+++ b/pcbnew/fp_tree_synchronizing_adapter.cpp
@@ -67,7 +67,12 @@ TOOL_INTERACTIVE* FP_TREE_SYNCHRONIZING_ADAPTER::GetContextMenuTool()
 bool FP_TREE_SYNCHRONIZING_ADAPTER::IsContainer( const wxDataViewItem& aItem ) const
 {
     const LIB_TREE_NODE* node = ToNode( aItem );
-    return node ? node->m_Type == LIB_TREE_NODE::TYPE::LIBRARY : true;
+
+    if( !node )
+        return true;
+
+    return node->m_Type == LIB_TREE_NODE::TYPE::LIBRARY
+        || node->m_Type == LIB_TREE_NODE::TYPE::GROUP;
 }
 
 
@@ -134,6 +139,10 @@ void FP_TREE_SYNCHRONIZING_ADAPTER::Sync( FOOTPRINT_LIBRARY_ADAPTER* aLibs )
 
     if( m_libMap.size() > count )
         m_tree.AssignIntrinsicRanks( m_shownColumns );
+
+    // Re-apply grouping if active (updateLibrary flattens groups before syncing).
+    if( !GetGroupColumn().IsEmpty() )
+        rebuildGroupNodes();
 }
 
 
@@ -145,6 +154,30 @@ int FP_TREE_SYNCHRONIZING_ADAPTER::GetLibrariesCount() const
 
 void FP_TREE_SYNCHRONIZING_ADAPTER::updateLibrary( LIB_TREE_NODE_LIBRARY& aLibNode )
 {
+    // Flatten any GROUP nodes back to direct ITEM children before syncing.
+    // Groups will be rebuilt at the end of Sync() via rebuildGroupNodes().
+    {
+        LIB_TREE_NODE::PTR_VECTOR flatChildren;
+
+        for( auto& child : aLibNode.m_Children )
+        {
+            if( child->m_Type == LIB_TREE_NODE::TYPE::GROUP )
+            {
+                for( auto& item : child->m_Children )
+                {
+                    item->m_Parent = &aLibNode;
+                    flatChildren.push_back( std::move( item ) );
+                }
+            }
+            else
+            {
+                flatChildren.push_back( std::move( child ) );
+            }
+        }
+
+        aLibNode.m_Children = std::move( flatChildren );
+    }
+
     // Re-enumerate from disk if the library has changed since it was last preloaded.
     // This picks up external modifications such as git branch switches.
     m_libs->RefreshLibraryIfChanged( aLibNode.m_Name );
diff --git a/eeschema/sch_io/database/sch_io_database.cpp b/eeschema/sch_io/database/sch_io_database.cpp
--- a/eeschema/sch_io/database/sch_io_database.cpp
+++ b/eeschema/sch_io/database/sch_io_database.cpp
@@ -601,6 +601,7 @@
         fieldsMap[field->GetName()] = field;
 
     static const wxString c_valueFieldName( wxS( "Value" ) );
+    static const wxString c_referenceFieldName( wxS( "Reference" ) );
     static const wxString c_datasheetFieldName( wxS( "Datasheet" ) );
     static const wxString c_footprintFieldName( wxS( "Footprint" ) );
 
@@ -630,6 +631,9 @@
 
         wxString value( strValue.c_str(), wxConvUTF8 );
 
+        if( mapping.name_wx == c_referenceFieldName && value.IsEmpty() )
+            continue;
+
         if( mapping.name_wx == c_valueFieldName )
         {
             SCH_FIELD& field = symbol->GetValueField();
diff --git a/kicad/project_tree_pane.cpp b/kicad/project_tree_pane.cpp
--- a/kicad/project_tree_pane.cpp
+++ b/kicad/project_tree_pane.cpp
@@ -113,6 +113,11 @@ static const wxChar* s_allowedExtensionsToList[] =
 {
     wxT( "^.*\\.pro$" ),
     wxT( "^.*\\.kicad_pro$" ),
+    wxT( "^.*fp-lib-table$" ),
+    wxT( "^.*sym-lib-table$" ),
+    wxT( "^.*design-block-lib-table$" ),
+    wxT( "^.*\\.kicad_dbl$" ),
+    wxT( "^.*\\.json$" ),
     wxT( "^.*\\.pdf$" ),
     wxT( "^.*\\.sch$" ),           // Legacy Eeschema files
     wxT( "^.*\\.kicad_sch$" ),     // S-expr Eeschema files
